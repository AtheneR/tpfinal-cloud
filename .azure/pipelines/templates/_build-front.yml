steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '20.x'
    displayName: 'Install Node.js'

  - script: npm ci
    displayName: 'Clean install'

  - script: npm run build --build-arg VITE_BUILD_ID=$(Build.SourceVersion) 
    displayName: 'Build and run of the project'